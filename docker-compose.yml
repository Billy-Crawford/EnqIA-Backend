version: "3.9"

services:

  backend:
    build: .
    container_name: enqia-backend

    ports:
      - "5000:5000"

    volumes:
      - .:/app

    env_file:
      - .env

    restart: unless-stopped

    